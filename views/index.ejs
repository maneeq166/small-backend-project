<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
  <div class="main p-3 w-full min-h-screen bg-zinc-900">
    <div class="form" >
      <form  action="/create" method="post" class="">
        <input class=" block w-1/2 bg-zinc-700 outline-none rounded-md p-3 mt-3" type="text" name="title" placeholder="Title goes here">
        <textarea class=" resize-none w-[700px] block bg-zinc-700 mt-3 p-3 h-[150px] rounded-md outline-none" name="details" placeholder="Text goes here" id=""></textarea>
        <input type="submit" value="Create task" class="bg-red-500 mt-3 px-3 text-white py-1 rounded-md">
      </form>
    </div>
    <%if(files.length > 0){%>
      <%files.forEach (function(val){%>
        <div class="bg-zinc-700 w-1/3 px-1 rounded-md mt-5 tracking-tighter py-4">
          <h1 class="text-bold text-white px-3 text-3xl "><%= val %><h1>
            <div class="flex justify-between px-3 ">
              <a href="/files/<%=val%>" class="text-blue-600" >Read more</a>
              <a href="/edit/<%=val%>" class="text-gray-400">edit</a>
            </div>
        </div>
        <% })%>
      <% } else {%>
        <h3 class="text-zinc-500 tracking-tighter bg-zinc-800 rounded-md text-3xl mt-5 p-4 w-[180px] "> no tasks yet.</h3>
        
        <%}%>
    
   
</body>
</html>